extends page

block prepend nav
  activeItem = 'withdraw'
  loggedIn = true

block containerContents
  .page-header
    h1 Spend Money 
      small Choose wisely.
  .row
    .span3
      form.withdraw-form
        p.lead $#{withdrawAmount} 
          button.btn(type='submit') Spend
    .span3
      p.lead $#{user.allocatedAmount} Available
  .row
    for bucket in buckets
      include ../components/buckets/bucket